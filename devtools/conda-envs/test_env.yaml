name: test
channels:
  - conda-forge
  - defaults
  - pytorch
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - gemmi
  - openmm
  - openmmtools
  - scipy
  - pytorch
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

