name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - gemmi
  - openmm
  - openmmtools
  - scipy
  - pymbar
  - matplotlib
    # Testing
  - pytest
  - pytest-cov
  - codecov
    # Pip-only installs
  - pip:
    - FastMBAR

