# readthedocs.yml

version: 2

build:
  image: latest

sphinx:
  configuration: docs/conf.py

python:
  version: 3.8
  install:
    - requirements: docs/requirements.yaml
    - method: pip
      path: .

conda:
  environment: docs/requirements.yaml
